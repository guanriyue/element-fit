---
description: FitSwitch 大量实例下的测量性能观测页面。
outline: false
pageType: doc-wide
---

import { FitSwitchStressTest } from '@/stress-tests/fit-switch';

# FitSwitch 压力测试

页面可以渲染 20、50、100、200、300 或 500 个实例，并在 `FitSwitch` 与 CSS Container Query 基线之间切换。可以批量改变 Expanded view 的内容宽度或容器宽度，观察模式切换时的 FPS 变化。

<FitSwitchStressTest />
