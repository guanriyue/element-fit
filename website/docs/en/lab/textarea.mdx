---
description: Observe automatic-height Textarea measurement performance across many instances.
outline: false
pageType: doc-wide
---

import { TextareaStressTest } from '@/stress-tests/textarea';

# Textarea Stress Test

This page can render 20, 50, 100, 200, 300, or 500 instances and switch between `Textarea` with `autoSize` and a CSS `field-sizing: content` baseline. Change controlled content, container widths, or remount instances in bulk to observe FPS during automatic-height measurement. The CSS baseline depends on the current browser's support for `field-sizing`.

<TextareaStressTest />
